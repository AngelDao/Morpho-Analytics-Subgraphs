{
  "name": "Morpho-Analytics-Subgraph-Aave",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ morpho-labs/Morpho-Analytics-Subgraph-Aave",
    "create-local": "graph create --node http://localhost:8020/ morpho-labs/Morpho-Analytics-Subgraph-Aave",
    "remove-local": "graph remove --node http://localhost:8020/ morpho-labs/Morpho-Analytics-Subgraph-Aave",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 morpho-labs/Morpho-Analytics-Subgraph-Aave"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.24.0",
    "@graphprotocol/graph-ts": "0.24.0"
  }
}
